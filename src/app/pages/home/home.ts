import { Component } from '@angular/core';
import { Slider } from '../../components/slider/slider';

@Component({
  selector: 'app-home',
  templateUrl: './home.html',
  imports: [Slider],
})
export class Home {}
